name: syncmate_windows
description: SyncMate 局域网多端文件与剪切板同步助手（Windows 端）。
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  shared_preferences: ^2.2.2
  syncmate_core:
    path: ../../packages/syncmate_core

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
